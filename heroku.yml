setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile          
    maildev: maildev.Dockerfile 

run:
  web: /opt/startup.relational.dev.sh  
  maildev: maildev --web 8081 --smtp 8025  
